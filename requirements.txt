# What the TWICE-DAILY BOT needs, and nothing else.
#
# Verified by walking the imports of webapp/once.py: the decision path never
# touches engine/core.py, so numba is not needed to run the bot. That is worth
# knowing - it means the always-on machine can be very small.
#
# The quarterly selection is heavier and runs elsewhere; see
# requirements-research.txt.
pandas>=2.2
numpy>=1.26
pyarrow>=14          # parquet panels
